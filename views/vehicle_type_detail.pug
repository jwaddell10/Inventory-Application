extends layout

block content
    h1 Vehicle Type Detail

    div(style='margin-left:20px;margin-top:20px')
        h4 Vehicles
        if type.length 
            dl
                each vehicleType in type
                    dt #{vehicleType.type}